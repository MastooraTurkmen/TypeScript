// ---------
// Enums 101
// ---------

enum Priority {
  Lowest = 0,
  Low = 1,
  Medium = 2,
  High = 3,
  Urgent = 4,
}

function addTicket(dtails: string, priority: number) {
  if (priority === Priority.Lowest) {
  }
  if (priority === 1) {
  }
  if (priority === 2) {
  }
  if (priority === 3) {
  }
  if (priority === 4) {
  }
}

addTicket("fix computer", Priority.Urgent);
